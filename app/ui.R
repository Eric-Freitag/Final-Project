library(shiny)

ui <- fluidPage(
    tabsetPanel(
        # Each of us will edit one tab panel
        
        tabPanel(),
        
        tabPanel(),
        
        tabPanel()
    )
)

shinyUI(ui)